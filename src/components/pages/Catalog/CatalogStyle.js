import styled from 'styled-components';

const CatalogStyle = styled.div`
  display: flex;
  flex-direction: row;
  flex-wrap: wrap;
  & > div {
    margin: 10px;
  }
`;

export default CatalogStyle;
